import React from "react";
import ServicePages from "../../Components/ServicePages";
import { servicesCategory } from "../../utlis/variables";

const SeoService = () => {
  return <ServicePages type={servicesCategory.SEO_SERVICES} />;
};

export default SeoService;
